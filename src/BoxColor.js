import React from "react"

function BoxColor({r, g, b}) {
    return (
        <div className="fill" style={{backgroundColor: `rgb(${r}, ${g}, ${b})`}}>
        whathever 
     </div>
    )

}

export default BoxColor